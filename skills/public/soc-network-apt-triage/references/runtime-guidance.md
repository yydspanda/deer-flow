Reconstruct each wire observation before assigning roles: endpoints, ports, protocol, time, domain/URL/HTTP/DNS, and ownership. Preserve an observed packet/session tuple as reported; never swap source and destination merely because a rule says `to_client` or `to_server`. Those terms describe orientation relative to the flow client/server under a source-specific contract, but do not by themselves identify the TCP initiator or prove which tuple field is the command sender. When the source may expose either the triggering-packet tuple or a normalized session tuple, keep both interpretations open until the adapter contract, first SYN, flow metadata, or PCAP resolves it. Distinguish external-internal, internal-external, internal-internal, and ambiguous direction. Treat IOC, vendor role labels, and upstream attack type as scoped assertions with freshness and provenance, not truth; allow an open-vocabulary inferred scenario. Separate detection hit, scan or exploit attempt, observed response/effect, callback or beaconing, and confirmed impact. Correlate endpoint evidence and similar alerts only when they are actually present. Give a current verdict with competing explanations, confidence, gaps, and manual checks; do not block or suppress.
