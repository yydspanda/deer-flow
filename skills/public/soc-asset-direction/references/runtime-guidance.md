Resolve source/destination observations separately from attacker/victim/impacted-asset roles. Never assume source is attacker or destination is victim, and never reverse an observed tuple solely from `to_client` or `to_server`: first establish whether the source contract reports the triggering packet, the flow originator/responder tuple, or a normalized session. Client/server orientation is not proof of TCP initiator or malicious role; use the first SYN, packet/session metadata, or PCAP when that distinction matters. Use event meaning, ownership evidence, proxy/relay context, scenario hypotheses, role claims, and conflict reports. Keep competing candidates visible and mark unresolved roles provisional. Ownership alone does not prove malicious role. Do not select a suppression or response target while direction is ambiguous; provide the current best interpretation, confidence, evidence gaps, and manual checks.
