schema_version: soc.enrichment_composition.v1
enabled: true
required_result_mode: mock
policy:
  schema_version: soc.enrichment_policy.v1
  policy_version: example-tenant-dev-mcp-enrichment-v1
  tenant_id: example-tenant
  enabled_routes:
    - asset.lookup
  asset_route: asset.lookup
  max_actions_total: 1
  max_actions_per_route: 1
retry_policy:
  max_attempts_per_action: 3
  stale_after_seconds: 300
bindings:
  - route: asset.lookup
    action: asset.lookup
    adapter_id: asset-lookup-soc-dev-mcp
    adapter_kind: mcp
